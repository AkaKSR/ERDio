<Window x:Class="ERDio.RelationshipDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Relationship" 
        Height="300" Width="400"
        Background="#2D2D30"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Text="Source Table:"/>
        <ComboBox x:Name="SourceTableCombo" Grid.Row="0" Grid.Column="1" 
                  DisplayMemberPath="Name" SelectionChanged="OnSourceTableChanged"/>
        
        <TextBlock Grid.Row="1" Text="Source Column:"/>
        <ComboBox x:Name="SourceColumnCombo" Grid.Row="1" Grid.Column="1"
                  DisplayMemberPath="Name"/>
        
        <TextBlock Grid.Row="2" Text="Target Table:"/>
        <ComboBox x:Name="TargetTableCombo" Grid.Row="2" Grid.Column="1"
                  DisplayMemberPath="Name" SelectionChanged="OnTargetTableChanged"/>
        
        <TextBlock Grid.Row="3" Text="Target Column:"/>
        <ComboBox x:Name="TargetColumnCombo" Grid.Row="3" Grid.Column="1"
                  DisplayMemberPath="Name"/>
        
        <TextBlock Grid.Row="4" Text="Relationship Type:"/>
        <ComboBox x:Name="RelationTypeCombo" Grid.Row="4" Grid.Column="1">
            <ComboBoxItem Content="One to One" Tag="OneToOne"/>
            <ComboBoxItem Content="One to Many" Tag="OneToMany" IsSelected="True"/>
            <ComboBoxItem Content="Many to One" Tag="ManyToOne"/>
            <ComboBoxItem Content="Many to Many" Tag="ManyToMany"/>
        </ComboBox>
        
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="OnCancel"
                    Background="#3E3E42" Foreground="#E0E0E0" BorderBrush="#555" Padding="10,5"/>
            <Button Content="Add" Width="80" Click="OnAdd"
                    Background="#007ACC" Foreground="White" BorderBrush="#005A9E" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>
